import React from 'react';

const Layout = ({ children }) => {
    return (
        <>
            <header className="App-header">
                <nav>
                
                </nav>
            </header>
            {children}
        </>
    )
}

export default Layout;
